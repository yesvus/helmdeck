import Link from "next/link";
import { demoActivity, demoOrders } from "../../demo-workspace";

const metrics = [["Gross sales", "$12,840", "+12.8%"], ["Orders", "184", "+8.2%"], ["Customers", "96", "+14.1%"], ["Conversion", "3.42%", "+0.6%"]];

export default function DashboardPage() {
  return <div className="space-y-6">
    <header className="flex flex-wrap items-end justify-between gap-4"><div><p className="text-sm text-zinc-500">Thursday, September 24, 2026</p><h1 className="mt-1 text-2xl font-bold tracking-tight">Good morning, Alex</h1><p className="mt-1 text-sm text-zinc-500">Here’s what’s happening at Northstar Supply.</p></div><Link href="/shell/products/new" className="rounded-lg bg-brand-600 px-4 py-2.5 text-sm font-semibold text-white hover:bg-brand-500 focus-visible:outline-2 focus-visible:outline-offset-2">Create product</Link></header>
    <section aria-label="Workspace performance" className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{metrics.map(([label, value, delta]) => <article key={label} className="rounded-xl border border-zinc-200 bg-white p-5"><p className="text-sm text-zinc-500">{label}</p><p className="mt-3 text-2xl font-bold">{value}</p><p className="mt-2 text-xs font-semibold text-emerald-700">{delta} <span className="font-normal text-zinc-500">vs. last month</span></p></article>)}</section>
    <div className="grid gap-6 xl:grid-cols-[1.5fr_1fr]"><section className="rounded-xl border border-zinc-200 bg-white p-5"><div className="flex items-center justify-between"><div><h2 className="font-semibold">Recent orders</h2><p className="mt-1 text-sm text-zinc-500">Your latest customer activity</p></div><Link href="/shell/orders" className="text-sm font-semibold text-brand-600 hover:underline">View all</Link></div><div className="mt-5 overflow-x-auto"><table className="w-full min-w-[480px] text-left text-sm"><thead className="text-xs text-zinc-500"><tr><th className="pb-3 font-medium">Order</th><th className="pb-3 font-medium">Customer</th><th className="pb-3 font-medium">Total</th><th className="pb-3 font-medium">Status</th></tr></thead><tbody>{demoOrders.slice(0, 3).map((order) => <tr key={order.id} className="border-t border-zinc-100"><td className="py-3 font-semibold">{order.id}</td><td>{order.customer}</td><td>{order.total}</td><td><span className="rounded-full bg-emerald-50 px-2.5 py-1 text-xs font-medium text-emerald-700">{order.status}</span></td></tr>)}</tbody></table></div></section>
    <section className="rounded-xl border border-zinc-200 bg-white p-5"><h2 className="font-semibold">Workspace activity</h2><ul className="mt-4 divide-y divide-zinc-100">{demoActivity.map(([event, when]) => <li key={event} className="py-3"><p className="text-sm">{event}</p><p className="mt-1 text-xs text-zinc-500">{when}</p></li>)}</ul><Link href="/shell/analytics" className="mt-2 inline-block text-sm font-semibold text-brand-600 hover:underline">Explore analytics</Link></section></div>
  </div>;
}
